name = "lisacreo-leads"
main = "src/index.js"
compatibility_date = "2024-09-23"

# Secret (set via CLI, never commit):
#   wrangler secret put RESEND_API_KEY

# Optional: bind to a custom route on your domain
# [[routes]]
# pattern = "lisacreo.pro/api/lead"
# zone_name = "lisacreo.pro"
